import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.antigravity.geminiocr',
  appName: 'Gemini OCR Receipt',
  webDir: 'dist'
};

export default config;
